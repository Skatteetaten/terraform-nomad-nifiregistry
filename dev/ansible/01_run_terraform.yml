- name: Terraform {{ mode }}
  terraform:
    project_path: ../../example/{{ mode }}
    force_init: true
    state: present
  register: terraform

- name: Terraform {{ mode }} stdout
  debug:
    msg: "{{terraform.stdout}}"